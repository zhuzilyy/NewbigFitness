// This file was generated by PermissionsDispatcher. Do not modify!
package com.example.gufei.bigfitness.com.GuFei.Ui.FragMent.Index;

import java.lang.String;
import permissions.dispatcher.PermissionUtils;

final class IndexFragMentPermissionsDispatcher {
  private static final int REQUEST_SCAN = 1;

  private static final String[] PERMISSION_SCAN = new String[] {"android.permission.CAMERA"};

  private IndexFragMentPermissionsDispatcher() {
  }

  static void ScanWithCheck(IndexFragMent target) {
    if (PermissionUtils.hasSelfPermissions(target.getActivity(), PERMISSION_SCAN)) {
      target.Scan();
    } else {
      target.requestPermissions(PERMISSION_SCAN, REQUEST_SCAN);
    }
  }

  static void onRequestPermissionsResult(IndexFragMent target, int requestCode, int[] grantResults) {
    switch (requestCode) {
      case REQUEST_SCAN:
      if (PermissionUtils.getTargetSdkVersion(target.getActivity()) < 23 && !PermissionUtils.hasSelfPermissions(target.getActivity(), PERMISSION_SCAN)) {
        return;
      }
      if (PermissionUtils.verifyPermissions(grantResults)) {
        target.Scan();
      }
      break;
      default:
      break;
    }
  }
}
